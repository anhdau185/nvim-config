local opts = { -- full default configs are in "plugins.configs.nvimtree"
  view = {
    adaptive_size = true,
    side = "left",
    width = 30,
    preserve_window_proportions = true,
  },
}

return opts
